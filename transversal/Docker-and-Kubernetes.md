# Docker & Kubernetes

[Docker](https://www.docker.com/) is an application container

Kubernetes is TODO

* concepts
  * container: code + dependencies to run the code (jar + JVM)
  * container technology allow to ensure that software setup remains stable 
    (e.g. with old versions of OS & dependencies; able to switch between projects with different needs)

*(Page mainly started in TODO 2023)*

[*Go to parent page*](../README.md)